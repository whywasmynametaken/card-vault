<div class="expform">

<h1>Expired Cards</h1>
<table id="myTable" class="row-border">
  <thead>
    <tr class="header">
      <th class='comp'>Company name</th>
      <th class='catagory'>Category</th>
      <th class='amount'>Amount</th>
      <th class='expire'>Expire</th>
    </tr>
  </thead>
  <tbody class='tbodycards'>
    <% @cards.each do |card| %>
      <tr class='card'>
         <td class='card_title'><strong><%= link_to card.company_name, card %></strong></td>
         <td class="extra"><i><%= card.category %> Card<i></td>
         <td class="extra"><% if card.amount? %>$<%= card.amount %><% end %></td>
         <td class="extra"><%= card.expire %></td>
         <!-- <td><%#= link_to 'Edit', edit_card_path(card) %></td> -->
         <!-- <td><%#= link_to 'Destroy', card, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
      </tr>

    <% end %>
  </tbody>
</table>
</div>

   </div>
